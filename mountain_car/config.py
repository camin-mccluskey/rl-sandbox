config = {
    "BATCH_SIZE": 500,
    # sets proportion boundaries for epsilon greedy approach
    "MAX_EPSILON": 0.9,
    "MIN_EPSILON": 0.01,
    # decay explore/exploit ratio
    "LAMBDA": 0.001,
    # discount factor for future rewards
    "GAMMA": 0.8
}
